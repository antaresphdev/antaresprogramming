---
title: Articles &middot; Antares Programming
description: Mga article tungkol sa web development para sa mga Filipino web developer
pagination:
  data: collections.posts
  reverse: true
  size: 12
permalink: /articles/{% if pagination.pageNumber > 0 %}{{ pagination.pageNumber + 1 }}/{% endif %}index.html
---

<h1 class="margin-inline--xl margin-block--none">Articles</h1>

<div class="padding--l cards-grid">
  {% set posts = collections.posts | reverse %}
  {% for item in pagination.items %}
    {% set hasImage = loop.index0 % 4 === 0 %}
    <article class="card{{ " has-image" if hasImage }}">
      {% if hasImage %}
        <a href="{{ item.url }}">
          {% respimg
            src = item.data.image,
            width = "600",
            height = "337.5",
            sizes = "300, 600, 900, 1200",
            alt = item.data.title,
            class = "card__image"
          %}
        </a>
      {% endif %}
      <div class="card__body">
        <h2 class="card__title">
          <a class="card__link" href="{{ item.url }}">{{ item.data.title }}</a>
        </h2>
        <p>{{ item.data.description }}</p>
        <div class="spaced cluster">
          {% if item.data.author and authors[item.data.author] %}
            {% set author = authors[item.data.author] %}
            <a href="/authors/{{ item.data.author }}/" class="fg-theme-color card__link">{{ author.name }}</a>
          {% else %}
            <span class="weight-bold">Anonymous</span>
          {% endif %}
          <time datetime="{{ item.date | machineReadableDate }}">{{ item.date | humanReadableDate }}</time>
        </div>
      </div>
    </article>
  {% endfor %}
</div>

<div class="center-justified cluster padding--l">
  {% if pagination.href.previous %}
    <a href="{{ pagination.href.previous }}" class="button">Previous</a>
  {% endif %}

  {% if pagination.href.next %}
    <a href="{{ pagination.href.next }}" class="button">Next</a>
  {% endif %}
</div>

<div class="stack padding--l text-align--center" style="--stack-gap: var(--space);align-items: center;">
  <p class="fs-5">Maging updated sa mga bago naming article!</p>
  <a href="/feed.xml" class="button primary">
  {% set feathericon = 'rss' %}
  {% include "components/feathericon.njk" %}
  <span>Mag-subscribe sa RSS feed namin</span>
  </a>
</div>